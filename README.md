## Day 2 - RGB LED

- Controlled onboard RGB LED with `neopixelWrite()`
- Tested red, green, blue, and off states
- Replaced blocking `delay()` logic with `millis()`
- Practiced non-blocking periodic control